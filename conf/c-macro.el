;;----------------------------------------------------------------------------;;
;; Dynamic Macro Settings
;; 「2回繰り返した動作」を自動検出し、
;; 以下繰り返しキー(本設定ではC-=)で繰り返すことができる。
;;----------------------------------------------------------------------------;;
(defconst *dmacro-key* [?\C-=] "繰返し指定キー")
(global-set-key *dmacro-key* 'dmacro-exec)
(autoload 'dmacro-exec "dmacro" nil t)
;;----------------------------------------------------------------------------;;
